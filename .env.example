# Server Configuration
PORT=7071

# Blob Storage URLs
# Format: BLOB_URL_<NAME>=<base-url>
# The <NAME> will be converted to lowercase and underscores replaced with hyphens
# Example: BLOB_URL_SAMPLE_BLOB becomes accessible as 'sample-blob'

# Example blob storage configuration
BLOB_URL_SAMPLE_BLOB=example.blob.core.windows.net

# You can add multiple blob storage URLs
# BLOB_URL_MY_STORAGE=my-storage.blob.core.windows.net
# BLOB_URL_ANOTHER_BLOB=https://another.blob.core.windows.net

# JWT Authentication Configuration
# JWT secret key for signing tokens
jwt_secret_key=your-secret-key-here

# JWT expiration time in seconds (default: 900 = 15 minutes)
jwt_expire_time=900

# Client credentials for authentication
# Format: <CLIENT_ID>_auth_client=<CLIENT_SECRET>
# Example: my-client_auth_client=my-secret
my_client_auth_client=my-secret
